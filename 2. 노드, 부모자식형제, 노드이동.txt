1. 노드
부모노드/자식노드/형제노드
tag를 표현하는 노드를 요소노드
문자를 표현하는 노드를 text노드
일반적으로 요소노드가 부모노드 text노드가 요소노드의 자식노드임
text노드는 따로 자식노드를 가질 수 없음(리프 노드, 잎사귀 노드)
총 12개의 노드 타입
돔트리는 보통 요소노드와 텍스트노드 이 두가지로 구성됨

2. 부모/자식/형제를 이용해 태그를 선택하는 방법
 태그.children[1] 태그.firstelementchild 태그.lastelementchild
 태그.parentelement
 태그.previouselementsibling 태그.nextelementsibling
 
 3. 문자인 text노드에 접근 하는 방법도 있긴 하지만 
 모든 노드가 공통으로 갖고 있는 프로퍼티를 활용한다면, 예상치 못한 텍스트 노드를 선택하게 되어 의도하지 않은 결과를 만들어 낼 가능성이 커지게 됩니다.
 자바스크립트로 웹 문서를 다룰 때는 아무래도 텍스트 노드보다는 요소  노드를 다룰 경우가 더 많을 겁니다.
만약 여러분이 의도하는 대상이 HTML 태그라면 요소 노드에 대한 이동 프로퍼티를 활용하는 것이 훨씬 더 안전하겠죠?
따라서 요소노드를 선택한다면 2번과 같이 요소노드들만 선택받을 수 있는 메서드를 활용하여 오류를 줄이자

# 프로퍼티가 속성이라면 메소드는 행동 예를 들어 listA 라는 array 가 있을 때, listA.length 는 property 죠? 그렇지만 listA.push(1) 은 method 입니다.
avascript 에서는 괄호로 함수를 호출하므로 대부분 괄호로 끝나면 method, 없으면 property 라 보아도 무방합니다.